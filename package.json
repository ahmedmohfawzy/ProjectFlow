{
  "name": "projectflow-server",
  "version": "1.0.0",
  "description": "ProjectFlow local SQLite backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --dev",
    "dev:ui": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "better-sqlite3": "^11.6.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}
